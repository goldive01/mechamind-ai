import Link from "next/link";
import { createMobileFieldService } from "@/services/mobile/mobileFactory";

export const dynamic = "force-dynamic";
export default async function MobileWorkOrdersPage({ searchParams }: { searchParams: Promise<{ assignedTo?: string }> }) { const { assignedTo } = await searchParams; const orders = await createMobileFieldService().dashboard(assignedTo); return <div className="space-y-5"><div><h1 className="text-2xl font-semibold">Work orders</h1><p className="mt-1 text-sm text-slate-500">Active field assignments and scheduled engineering work.</p></div><form className="flex gap-2"><input name="assignedTo" defaultValue={assignedTo ?? ""} placeholder="Filter by engineer" className="min-w-0 flex-1 rounded-xl border border-slate-300 bg-transparent px-3 py-2 text-sm dark:border-slate-700"/><button className="rounded-xl bg-cyan-600 px-4 py-2 text-sm font-medium text-white">Filter</button></form><div className="space-y-3">{orders.map((order) => <Link key={order.id} href={`/mobile/work-orders/${order.id}`} className="block rounded-2xl border border-slate-200 bg-white p-4 dark:border-slate-800 dark:bg-slate-900"><div className="flex items-start justify-between gap-3"><div><p className="font-semibold">{order.title}</p><p className="mt-1 text-xs text-slate-500">{order.assetId} · {order.assetName}</p></div><span className="rounded-full bg-slate-100 px-2 py-1 text-xs dark:bg-slate-800">{order.priority}</span></div><p className="mt-3 text-sm">{order.status} · {order.assignedTo ?? "Unassigned"}</p></Link>)}{!orders.length ? <p className="rounded-2xl border border-slate-200 p-5 text-sm text-slate-500 dark:border-slate-800">No active work orders found.</p> : null}</div></div>; }
