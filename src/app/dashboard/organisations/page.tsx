import { Card } from "@/components/Card";
import { PageHeader } from "@/components/PageHeader";
import { requireDashboardSession } from "@/lib/auth-session";
import { createOrganisationServices } from "@/services/organisationFactory";
import { createOrganisation, switchOrganisation } from "./actions";
export const dynamic = "force-dynamic";
const field = "rounded-xl border border-slate-300 bg-transparent px-3 py-2 text-sm dark:border-slate-700";
export default async function OrganisationsPage() { const session = await requireDashboardSession(); const organisations = await createOrganisationServices().organisations.listForUser(session.user.id); return <div className="space-y-6"><PageHeader title="Organisations" description="Select an operational tenant or create a new organisation."/><Card title="Your organisations"><div className="grid gap-3 md:grid-cols-2">{organisations.map(item => <form action={switchOrganisation} key={item.id} className="flex items-center justify-between rounded-xl border border-slate-200 p-4"><input type="hidden" name="organisationId" value={item.id}/><div><p className="font-semibold">{item.name}</p><p className="text-sm text-slate-500">{item.slug}</p></div><button className="rounded-lg border border-cyan-600 px-3 py-2 text-sm text-cyan-700">Use</button></form>)}</div></Card><Card title="Create organisation"><form action={createOrganisation} className="grid gap-3 md:grid-cols-3"><input name="name" required className={field} placeholder="Organisation name"/><input name="slug" required className={field} placeholder="organisation-slug"/><input name="description" className={field} placeholder="Description"/><button className="rounded-xl bg-cyan-600 px-4 py-2 text-sm font-semibold text-white">Create</button></form></Card></div>; }
