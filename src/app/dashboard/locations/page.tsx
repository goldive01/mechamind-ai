import { Card } from "@/components/Card";
import { PageHeader } from "@/components/PageHeader";
import { requireOrganisationScope } from "@/lib/auth-session";
import { createOrganisationServices } from "@/services/organisationFactory";
import { createArea, createBuilding, createSite } from "./actions";
export const dynamic = "force-dynamic";
const field = "rounded-xl border border-slate-300 bg-transparent px-3 py-2 text-sm dark:border-slate-700";
export default async function LocationsPage() { const { organisationId, organisation } = await requireOrganisationScope(); const services = createOrganisationServices(); const [sites, buildings, areas] = await Promise.all([services.sites.list(organisationId), services.buildings.list(organisationId), services.areas.list(organisationId)]); return <div className="space-y-6"><PageHeader title="Sites, buildings and areas" description={`Manage the location hierarchy for ${organisation.name}.`}/><div className="grid gap-4 xl:grid-cols-3"><Card title="Sites"><form action={createSite} className="grid gap-2"><input name="code" required className={field} placeholder="Code"/><input name="name" required className={field} placeholder="Name"/><input name="address" className={field} placeholder="Address"/><button className="rounded-xl bg-cyan-600 p-2 text-white">Add site</button></form>{sites.map(item => <p key={item.id} className="mt-3 text-sm">{item.code} · {item.name}</p>)}</Card><Card title="Buildings"><form action={createBuilding} className="grid gap-2"><select name="siteId" required className={field}>{sites.map(item => <option key={item.id} value={item.id}>{item.name}</option>)}</select><input name="code" required className={field} placeholder="Code"/><input name="name" required className={field} placeholder="Name"/><button className="rounded-xl bg-cyan-600 p-2 text-white">Add building</button></form>{buildings.map(item => <p key={item.id} className="mt-3 text-sm">{item.code} · {item.name}</p>)}</Card><Card title="Areas"><form action={createArea} className="grid gap-2"><select name="buildingId" required className={field}>{buildings.map(item => <option key={item.id} value={item.id}>{item.name}</option>)}</select><input name="code" required className={field} placeholder="Code"/><input name="name" required className={field} placeholder="Name"/><input name="description" className={field} placeholder="Description"/><button className="rounded-xl bg-cyan-600 p-2 text-white">Add area</button></form>{areas.map(item => <p key={item.id} className="mt-3 text-sm">{item.code} · {item.name}</p>)}</Card></div></div>; }
